# API server port. Defaults to 4000 when omitted.
PORT=4000

# Development Redis URI for the Docker Compose service.
REDIS_URL=redis://127.0.0.1:6379

# Development MongoDB URI for the Docker Compose service.
MONGODB_LOCAL_URI=mongodb://127.0.0.1:27017/pulseops

# Production MongoDB URI. Atlas remains supported by setting its mongodb+srv URI.
MONGODB_URI=

# Optional DNS server for MongoDB SRV record resolution. Defaults to 1.1.1.1.
MONGODB_DNS_SERVER=1.1.1.1

# Required secret used to sign and verify JWT access tokens.
JWT_SECRET=

# Optional JWT access-token lifetime. Defaults to 1h; use a whole number plus s, m, h, or d.
JWT_EXPIRES_IN=1h

# Optional GitHub REST API base URL. Defaults to https://api.github.com.
GITHUB_API_URL=https://api.github.com

# Optional personal access token or GitHub App token used to query repository metadata.
GITHUB_TOKEN=

# Optional GitHub webhook secret used to validate X-Hub-Signature-256 headers.
GITHUB_WEBHOOK_SECRET=
